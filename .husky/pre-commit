#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
echo "📝 Running lint-staged..."

# Prefer local install, but still works via npx if needed
npx lint-staged

# Run type checking
echo "🔷 Running TypeScript type checking..."
npm run check:types


# NOTE: Unit tests are currently failing in this repository; keep pre-commit lightweight.
# Run tests in CI or manually via: npm run test:unit

echo "✅ All pre-commit checks passed!"
