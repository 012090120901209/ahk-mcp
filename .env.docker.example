# Docker Environment Configuration for AHK MCP Server
# Copy this file to .env to customize your Docker setup

# Server Ports
SSE_PORT=3000
DEV_PORT=3001

# Logging
AHK_MCP_LOG_LEVEL=info  # debug | info | warn | error

# Node.js Settings
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=512

# Optional: Custom AutoHotkey paths (for volume mounts)
# AHK_SCRIPTS_DIR=/path/to/scripts
# AHK_INSTALL_DIR=/path/to/autohotkey

# Optional: Resource limits (override docker-compose.yml)
# MEMORY_LIMIT=512M
# CPU_LIMIT=1.0
